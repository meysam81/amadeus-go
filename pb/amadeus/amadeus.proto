syntax = "proto3";

package go.kit.srv.amadeus;

service AmadeusService {
    rpc Greeter (Request) returns (Response);

}

message Request {
    string name = 1;
}

message Response {
    string message = 1;
}
